# Runtime variables used by nginx template in frontend container.
# Set these in Railway frontend service variables.
# Replace `backend` with your exact backend service name in Railway.
BACKEND_HOST=${{backend.RAILWAY_PRIVATE_DOMAIN}}
BACKEND_PORT=${{backend.PORT}}

# Build-time value for frontend API base path.
# Keep /api when using nginx reverse proxy.
VITE_API_BASE_URL=/api

# Optional session strategy for refresh token persistence (local|session)
VITE_REFRESH_TOKEN_STORAGE=session
