# Runtime variable used by nginx template in frontend container.
# Set this in Railway frontend service variables.
BACKEND_UPSTREAM=backend.railway.internal:8000

# Build-time value for frontend API base path.
# Keep /api when using nginx reverse proxy.
VITE_API_BASE_URL=/api

# Optional session strategy for refresh token persistence (local|session)
VITE_REFRESH_TOKEN_STORAGE=local
